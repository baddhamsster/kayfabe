specVersion: "v1"
serviceId: "kayfabe-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kayfabe-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
